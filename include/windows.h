/*
// Update forward declaration for windows_main

#ifndef ANTIRANSOM_WINDOWS_H
#define ANTIRANSOM_WINDOWS_H

// Forward declaration of GlobalArgs
typedef struct {
    int daemon_mode;
    int verbose_mode;
    char config_path[512];
    char watch_directory[512];
} GlobalArgs;

// Windows platform entry point
int windows_main(int argc, char* argv[], const GlobalArgs* global_args);

#endif // ANTIRANSOM_WINDOWS_H

*/